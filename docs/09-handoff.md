# 当前接续点

用户已确认近现代林缘小镇、木石小屋、温暖微缩模型风格，并授权继续。当前 v0.2.0-dev.1 为第一批体验实现，不更换原型。

先读 README、开发日志与实际规则，再看 [体验规划](13-experience-plan.md)、[工作排期](14-work-schedule.md)、[美术规范](15-art-direction.md)。11、12 保留历史提案；14 是进度唯一入口。

## 已实现

- 标题、准备、预警、火情 / 暂停、结算、修改重试贯通。
- 直接搬迁、取消、40 步会话撤销；费用、合法性与喷淋范围提示。
- 木石屋、蓄水喷淋、林缘树、实际降温环、亮窗与可选合成音。
- 17 项规则检查、构建及本地生产子路径浏览器流程通过。人类偏好、声音听感、真实手机与低端性能未验收。

## 下一批

S2 优先解决局部保护、分区和设施取舍，补有希望的初始布局、挡火和过载反馈。六格防火带满保、缺格几乎全灭的问题仍存在。S3 安排首次玩家体验并修复；S4 按授权处理评审访问与 Pages。仓库保持私有。

## 代码与同步

`src/sim/model.ts` 为固定步进规则；新增 cooling 只暴露实际分配量。`model.test.ts` 有 17 项检查。`src/view.ts` 为模型和表现，`src/main.ts` 为流程、交互与存档，`src/audio.ts` 为合成音，`src/style.css` 为布局。部署模板仍为 `docs/deployment/pages.yml`，未启用。

接续前 `git status`、`git pull --ff-only`，使用 Node 24.13.1 和 `npm ci`。Git 不同步浏览器布局或聊天全文。同步可分享的设计依据、真实进展与验证记录，不把预算当实际工时。
