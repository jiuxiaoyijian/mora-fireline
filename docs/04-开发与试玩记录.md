# 开发、验证与用时记录

## 记录原则

- 计划、自动化检查、浏览器人工操作检查、真人试玩分别说明。
- 没有实际观察的数据不填写为结果。
- 本地时间使用 Asia/Shanghai；工具原始 UTC 时间可用于核对。
- 自然经过时间不等于有效工作时间；包括等待时需说明。

## 2026-09-14 · 题目分析

- 完成：阅读并视觉核对三页 PDF；核实静态部署路径；整理选题比较和首版方案。
- 验证：PDF 文本和三页渲染结果一致；官方部署文档支持纯前端路线。
- 未完成：所有代码、玩法验证和部署。
- 实际用时：未精确记录，不填估计数冒充实际耗时。

## 2026-09-15 · 文档与协作初始化

- 可核验时间点：00:02:09（UTC 2026-09-14 16:02:09），本轮已完成环境和 GitHub 登录检查。
- 环境：现有工作目录已经初始化 Git，但无提交、无远端；可使用 Node、npm、Git、GitHub CLI。
- 完成：README、题目解读、设计决策、对话摘要、协作指南与来源清单初版。
- 完成：忽略临时 PDF 渲染、依赖、构建输出及本地配置。
- 完成：初始文档提交 `5585c99`；建立私有仓库 `jiuxiaoyijian/mora-fireline`；验证本地与远端 `main` 提交相同，并成功在独立目录克隆。
- 然后按用户指定顺序进入规则细化与原型实现。

## 2026-09-15 · v0.1 可玩原型

### 实际实现

- TypeScript + Three.js + Vite，Node 24.13.1；依赖已锁定。
- 8×8 低多边形 3D 街区，12 栋住宅，12 点防灾预算。
- 住宅放置与搬迁、防火带、共享降温消防站、拆除退款。
- 45 秒确定性火灾；固定 0.1 秒步长；暂停、2× 加速与帮助弹窗暂停。
- 建造、运行、结算、调整、原样重试；同场景上下次结果比较。
- 真实点燃事件、传播来源、地图路径开关、消防覆盖开关。
- 鼠标 3D 拾取、键盘操作网格、焦点保留、布局本地保存及数据合法性校验。
- 生产构建使用相对资产路径；提供未启用的 Pages 工作流模板。

### 规则问题与迭代证据

| 问题 / 观察方式 | 修改 | 可核验结果 |
| --- | --- | --- |
| 检查拓扑并自动模拟发现，两个孤立火源可用两格防火带完全封堵 | 西侧改为不可建造的连续干草带 | 两格封堵现在保住 0 栋，连续隔离保住 12 栋 |
| 空地如果不可燃，会免费替代防火设施（设计检查） | 空地保留可燃植被，只有防火带不传播 | 默认布局全部失守，布局改变影响传播 |
| 最新 Three.js 浏览器警告 PCFSoftShadowMap 已移除 | 改用 PCFShadowMap | 生产构建浏览器无此警告 |
| 桌面 720 像素高视口下启动按钮偏低（截图观察） | 增加低高度布局，缩短顶部与画布高度 | 同一视口可直接看到启动按钮 |
| 事件坐标不易对应地图（界面检查） | 增加 A–H 与 1–8 坐标标签 | 3D 图与点燃事件采用同一坐标 |
| 重建键盘网格可能丢失操作焦点（代码检查） | 重建后恢复当前格子焦点 | 浏览器 Enter 建造后焦点仍为原格子 |

### 自动化检查

`npm test`：16 / 16 通过，涵盖默认失败、两种可行防灾布局、简化解回归、完整事件确定性、分批步进一致、重置、结束幂等、住宅库存、预算退款、替换设施、边界保护、存档校验、风向差异、截止时燃烧判定及事件来源。

`npm run build`：类型检查和 Vite 生产构建通过。Three.js 分包约 543 kB（gzip 约 135 kB），触发 500 kB 提示；不是构建错误，尚未进行低端设备性能验收。

### 桌面浏览器实际操作检查

工具：Codex 内置浏览器，通过页面控件、鼠标和键盘操作；这属于 AI 执行的浏览器 QA，不属于外部玩家试玩。

| 检查 | 结果 |
| --- | --- |
| 默认街区运行、暂停、继续 | 暂停时保持 38.4 秒，继续后到 45.0 秒结算 |
| 首次默认布局 | 0 / 12 栋，结果与规则模拟一致 |
| 查看复盘来源 | 例如 3.7 秒 B3 草地 → C3 住宅，来自实际事件 |
| 返回建造，铺设 B2–B7 防火带 | 预算从 12 降至 6，住宅保持 12 栋 |
| 第二次同火情实验 | 12 / 12 栋，显示上次 0 → 本次 12，多保住 12 栋 |
| 生产构建子路径 `/mora-fireline/` | 页面与 3D 资源加载成功，可完成一轮实验 |
| 点击三维地图的 B3 格 | 实际铺设防火带，预算降至 11 |
| 刷新布局恢复 | 预算仍为 11，修改未丢失 |
| 清空布局 | 开始按钮禁用，提示还需安置 12 栋 |
| 默认街区恢复与玩法说明 | 可重新开始，帮助开关正常；运行中打开帮助会暂停 |
| 键盘 B3 格 Enter 建造消防站 | 预算变为 8，焦点仍保留在该格 |
| 消防覆盖开关 | 控件进入开启状态；详细保护效果由模拟检查覆盖 |
| 生产构建控制台 | 未捕获新的 warning/error |

### 尚未完成

- 外部首次玩家试玩、难度和重复游玩动机验证。
- 真实移动设备、低端设备、不同浏览器的兼容与性能验收。
- 仓库公开范围与评审访问权限确认。
- 正式 GitHub Pages 发布及正式公网链接验收。

### 可核验时间记录

本轮工具时间点覆盖 2026-09-15 00:02:09 至 00:26:29（Asia/Shanghai），自然经过 **24 分 20 秒**。这个区间覆盖文档、建仓、原型实现和主要检查，也包含工具执行与等待；不包含记录起点之前的短暂检查，以及之后的整理和最终同步。未单独测量人类有效工时，不能将该区间等同于完整开发工时。

最终原型提交为 `82d59e9`，已快进合并并推送到 `main`。本地、GitHub 远端和独立克隆目录都已取得同一原型版本，工作目录干净。2026-09-15 **00:29:13** 核对完成并准备本地预览，测量区间累计 **27 分 04 秒**（从 00:02:09 起，包含工具等待，不含此前检查及这条收尾记录的提交）。

## 2026-09-15 · 按用户 skill 做选型复审

- 输入：用户提供的 `game-prototype-triage-SKILL.md`；用户要求审核当前方案并考虑新的原型。
- 读取：完整 skill、README、规则、开发与决策记录、模拟与界面代码。审查开始时 Git 工作目录干净，基线为 `0a0d79f`。
- 新增：`docs/10-原型审核与候选比较.md`、只读测量脚本 `scripts/triage-probes.ts`、原始结果 `docs/evidence/triage-v01.json`。
- 测量范围：默认住宅不动；24 个单消防站位置、276 对双消防站位置、64 个西侧防火带子集，另测默认布局与完整隔离基准。不是全部布局空间的穷举。
- 新证据：默认住宅 3.7 秒开始起火、9.6 秒全部起火、19.6 秒全部烧毁，固定结算为 45 秒；防火带子集为 63 个零保护、1 个满保护；双消防站 26 对达标，其中 15 对满保护。
- 判断：优先修复等待和二元反馈，避免用「存在多解」代替「策略有取舍」。当前交付推荐任务化防灾实验；完全重选推荐先验证双视点异常观察。
- 复现：`node --experimental-strip-types scripts/triage-probes.ts`。本轮没有修改游戏运行时代码，因此不重复整套游戏测试或浏览器 QA；既有测试和浏览器记录属于此前版本验证。
- 未进行：候选实现、真人试玩、仓库公开或公网部署。
- 时间：工具记录点为 2026-09-15 21:47:53（Asia/Shanghai，UTC 13:47:53）。本轮未记录完整起止区间，不报告完整用时。

## 2026-09-15 · 继续当前方向与后续计划

- 用户明确继续《火线街区》，要求先提供玩法体验和完成度提升的目标与优先级。
- 已读取 README、开发记录、实际规则、接续说明和此前审核证据；开始时工作目录干净，基线为 `b297cb1`。
- 新增 `docs/11-后续目标与优先级.md`：P0 结算节奏、渐进改善、设施取舍、可解释反馈与真人试玩；P1 关卡、建造便利、视听和完整流程；另列正式提交门槛与暂缓功能。
- 更新方向决定、对话摘要及接续说明，标注具体范围待用户评估。
- 本轮未改运行时代码或规则、未重新测量模拟、未进行游戏 QA 或真人试玩、未公开部署。文档变更仅做差异与链接检查。
- 工具时间点：2026-09-15 23:00:19（Asia/Shanghai）；未测量本轮完整有效工时。

## 2026-09-15 · 玩家吸引力与完整体验工作项

- 用户强调题材、美术、建设动机、开始界面、交互、单局与结算对吸引力的重要性，要求整理对应工作项。
- 已读取 README、开发记录、实际规则及上一版计划；本轮开始时工作目录干净，基线为 `311bd97`。
- 新增 `docs/12-玩家吸引力工作项.md`，包含题材备选、推荐动机、美术与声音目标、完整单局旅程、P0 / P1 工作包、验收与体验观察方法。
- 修正上一版优先级：首个单关建议同时具备题材、开始、基础建造手感、危机节奏和结算，规则修复继续保留；多关内容与装饰后置。
- 推荐林缘小镇与外部山火属于待评估提案，不代表用户已选定题材或游戏已改版。
- 本轮仅做文档一致性、链接和差异检查；未修改运行时代码、生成资产、重复测试或新增真人试玩。
- 工具时间点：2026-09-15 23:11:34（Asia/Shanghai）；未测量完整工时。

## 试玩记录模板（尚无真人试玩结果，继续待补充）

| 日期 / 版本 | 参与者及方式 | 观察到的问题 | 假设原因 | 修改 | 复测结果 |
| --- | --- | --- | --- | --- | --- |
| 待填写 | 待填写 | 待观察 | 待分析 | 待实施 | 待复测 |

## 用时记录模板

| 起止时间 | 工作范围 | 自然经过时间 | 等待/中断说明 | 有效工时 |
| --- | --- | --- | --- | --- |
| 2026-09-15 00:02:09–00:26:29 | 文档、GitHub 初始化、原型与主要检查 | 24 分 20 秒（测量区间） | 包括工具执行与等待；不是全流程完整工时 | 未单独计量 |
| 2026-09-15 00:26:29–00:29:13 | 键盘 QA、整理、生产构建、推送与独立克隆拉取 | 2 分 44 秒 | 包括工具执行与等待 | 未单独计量 |

## 2026-09-15 · v0.2 第一批：规划拆表与完整单局

### 范围

- 起始基线 `5e815ca`，工作目录干净；分支 `codex/player-experience`。
- 用户确认风格与继续实施。新增体验规划、排期、美术规范（13–15），旧提案加当前状态入口。
- v0.2.0-dev.1：实时场景标题、进入 / 继续、外部山火委托、2.4 秒预警、预警与火情暂停、标题返回、家园结果卡、完整步进查看结果。
- 空地直接搬迁、取消、最多 40 步会话撤销、合法性费用与喷淋范围提示；可选合成音。
- 木石住宅、蓄水罐喷头、场外林缘、降温环、结算亮窗。模拟仅新增 cooling 表现数据，地图和数值未调整。
- 旧“东风”用语改为“向东吹”，避免来向和传播方向混淆。
- A06.1 规则平衡未开始；模型虚影、落地动画、过载提示等仍标部分实现。没有下载第三方美术。

### 自动检查

- `npm test`：17 / 17 通过。原有 16 项持续通过，新增实际降温与纯覆盖区别检查，防止未受热住宅显示生效。
- `npm run build`：类型与生产构建通过。Three.js 分包约 544 KB，gzip 135 KB，仍有既有 500 KB 提示；未做低端性能实测。
- 布局存储仍兼容 v1；cooling 为运行态数据，不写入布局存档。

### 浏览器实际 QA（不是真人试玩）

采用本地生产构建 `/mora-fireline/` 子路径，独立于用户开发预览来源。本轮窗口包含横向与窄长视口，不等于真实手机验收。

| 检查 | 实际观察 |
| --- | --- |
| 标题与静态子路径 | 3D 与资源正常，身份、玩法承诺、继续入口可见 |
| 搬迁 C3 → C2 | C3 变草地，C2 变住宅，保持 12 / 12 |
| 撤销搬迁 | C3 恢复住宅，C2 恢复草地，资源恢复 |
| 预警暂停 → 标题 → 返回 | 模拟时间保持 00.0 秒，可继续原预警 |
| 默认布局查看结果 | 完整推进至 45.0 秒，保住 0；首起火 C3 为 3.7 秒 |
| B2–B7 六格防火带 | 花费 6 点，保住 12；比上局多 12 栋 |
| 原样重试，正常 2× 播放 | 保住 12，差值 0；与查看结果一致 |
| 最终构建刷新再跑 | 恢复六格防线，完整步进后 12 / 12 |
| 声音开关 | aria-pressed 可切换，未捕获新 warning / error；听感未验收 |
| 用户原布局 | 对比恢复预览与原先 64 格类型完全一致，原有 11 栋住宅及设施未被测试覆盖 |

真实截图：[标题](evidence/v02-title.png)、[结算](evidence/v02-outcome.png)。截图说明当前视觉状态，不是真人反馈或最终美术验收。

### 过程问题与恢复

- 旧本地预览服务已停止，刷新出现连接拒绝。重启后使用同一浏览器新标签恢复；未修改安全设置。
- 个别测试弹窗已不在可见状态，读取当前页面后从现有入口继续，没有盲目重复操作。
- 收尾换行整理脚本误将 Markdown 写空。已立即告知用户，从本轮开始时的 Git 基线恢复所有历史 Markdown，再重建本轮三份新增文档及状态/日志更新。游戏代码、截图、用户布局未受影响。恢复后重新检查历史保留、非空文件、链接与差异，不把误写状态提交。

### 剩余项与时间边界

S2 优先局部保护、设施取舍与有希望起局，再补挡火和负载反馈；S3 安排真实首次玩家及听感检查。真实移动设备、低端性能、公网 Pages 尚未验收。

工具核验时间点为 23:26:33、23:42:20（Asia/Shanghai）；前者不是任务最初起点，未完整计量有效工时，不报告本轮总工时。排期粗估不是实际时间。


## 文档中文命名整理

- 按用户要求将 docs 内 15 份 Markdown 文档改为中文文件名，保留 01–15 阅读编号。
- 同步修改 README、AGENTS 和文档中的链接及路径引用；截图、测量数据与部署配置保持原名。
- 仅文档命名与引用变更，检查文件非空、旧引用残留、内部链接和 Git 差异，不重复游戏测试。

## 2026-09-16 · 建造与灾害模拟资料研究

- 新增 [七款游戏参考资料](16-建造与灾害模拟竞品研究.md)，以官方／发行方商店和 UNDRR 页面为依据，记录检索日期与来源。
- 区分资料事实和设计建议，将局部保护、设施取舍、首笔建设、结算与重试映射到既有工作编号；同步 README 入口与对话摘要。
- 本轮没有修改代码、规则或资源，没有购买、安装或实玩参考游戏，没有新增真人试玩结论。
- 验证范围为文档非空、内部链接和 Git 差异；不重复运行与本次文档变更无关的游戏测试。未记录实际研究工时。

## 2026-09-16 · 多 Agent 分工规划

- 按用户要求，实际启动测试、美术、UI 三个只读子任务审查项目；主控编写协作规范，明确文件所有权、交接、稳定版本回归和首批工作分配。
- 审核发现 main/view 是主要并发写入冲突点；新增模块目录只是规划，尚未抽离或改造代码。共享材质释放、DOM 绑定、测试场景版本化和独立预览存储纳入交接注意项。
- 更新 README、工作排期和对话摘要；本轮仅检查文档差异、非空和链接，没有运行游戏回归或生成美术资源。

## 2026-09-16 · v0.2.0-dev.2 固定画幅与游戏内交互

### 实现与协作

- 用户指定固定分辨率游戏体验后，UI Agent 实现独立模板与样式，主控接入舞台缩放、模态栈、游戏流程和相机；美术 Agent 只读审查取景、拾取和渲染密度；测试 Agent 建立矩阵并独立执行命令检查。
- 改为 1920×1080 逻辑舞台等比适配，非 16:9 留边；地图全画幅，资源与工具作为 HUD。主菜单、帮助、暂停、确认、键盘网格和复盘位于游戏内。
- 主菜单往返保留会话，运行和预警自动暂停；新规划、清空、恢复默认与中途返回建造先确认。焦点限制在当前弹层，底层 inert 防止误操作。
- 保留摆放、直接搬迁、取消、撤销、固定步进、加速、跳过演出结算和存档规则。本轮无模拟数值改变，无外部资源引入。
- 文档 [交互设计](18-固定画幅与游戏内交互设计.md) 记录设计依据，README、实际规则、接续与排期同步更新。版本为开发版，不宣称完整 v0.2 完成。

### 验证证据

- 测试 Agent 在集成快照执行规则测试 17 / 17 和类型／构建检查通过；最后两处新规划状态与工具属性调整由主控补构建及针对性浏览器复测。
- 主控通过浏览器真实操作验收：1280×720 直接点击 B6 摆放及撤销；喷淋花费、住宅 C3→C2 搬迁与撤销；确认取消／执行；住宅未放齐不能开始；菜单和主菜单往返；默认布局 0 栋，六格防线 12 栋且展示改善。
- 六格防线 1×、2×完整播放和查看结果均到 45.0 秒保住 12；原样重试差值为 0。刷新保留布局、撤销历史重置。
- 1920×1080 帮助弹层完整；1440×900 画面为 1440×810，上下各留 45 像素，点击位置仍对应 B6；900×1200 等比展示无页面滚动。缩窄桌面视口不算真实触屏验收。
- 独立测试预览使用端口 4176 和 `/mora-fireline/` 子路径，不改用户原页面存档。最后读取的浏览器 error／warn 日志为空。
- 截图：[主菜单](evidence/固定画幅-主菜单.png)、[建造画面](evidence/固定画幅-建造.png)、[结算](evidence/固定画幅-结算.png)。完整矩阵见 [QA 记录](qa/固定画幅交互回归.md)。

### 问题、修复与限制

- 首次预览未配置子路径，相对资源在子路径下无法载入；改用显式带 base 的 Vite 预览后正常。生产资源使用相对路径，仓库 Pages 兼容要求保留。
- 新规划最初保留上次工具，已重置为防火带并关闭覆盖／路径；场景工具状态改为独立 data-active-tool，避免混入按钮选择器。最终针对性复测确认工具按钮恰为四个且状态正确。
- Three.js 546.15 kB（gzip 135.63 kB）包体提示仍存在，构建成功。内嵌环境全屏切换未能确认，运行期失焦本轮未定向复测；真实手机、低端性能、听感和真人趣味性仍未验收。
- 未发布公网 Pages。用户中途要求额度重置后继续，延续本任务，没有消耗重置积分。无精确有效工时记录。

## 2026-09-16 · 本地工程接续与全毁收束

### 分析与实现

- 起始基线 `fc094c9`，工作区干净；快进拉取检查显示已是远端最新。读取工程说明、实际规则、开发日志与排期，新增 [工程结构与接续分析](19-工程结构与接续分析.md)。
- 技术分层已具备独立确定性模拟、Three.js 表现与游戏内 UI；当前主要产品缺口仍为 A06.1 的分区收益及设施取舍。
- 完成 A05.2 的一个明确子项：新增 `src/sim/playback.ts`，12 栋住宅全部烧毁后自动推进剩余规则步并结算。手动查看结果复用完整推进函数。默认局在 19.6 秒触发，减少 25.4 秒模拟演出（1× 时的标称播放时长，不是本轮实际工时）。
- 自动与手动结算都保留 450 步完整终态与事件。结果页、详细复盘补充自动收束时刻；新局重置该标记，避免手动或成功结算残留提示。玩法说明同步更新。
- 没有修改地图、预算或热量数值。未将一般稳定状态、局部保护和设施取舍写成完成；未推送或部署。

### 环境与自动验证

- 本机 Node 24.19.0，处于声明的 Node >=24 范围内，但并非 `.nvmrc` 指定的 24.13.1。系统未直接提供 npm，通过运行时附带 pnpm 临时调用 npm，按现有 package-lock 执行 `npm ci`；锁文件未改变。
- 基线 17 / 17 通过；新增 5 项检查后 22 / 22 通过。覆盖全毁触发点、完整事件与状态等价、燃烧期不能提前跳过、局部／全部保护继续完整播放、手动跳过幂等、空图不误判。
- 类型检查、生产构建、`git diff --check` 通过；最终 CSS 已补生产构建。Three.js 分包仍为 546.15 kB（gzip 135.63 kB），既有体积提示保留。
- 重跑定向测量脚本：64 个西侧隔离子集仍为 63 个保住 0 栋、1 个保住 12 栋；24 个单喷淋最佳 3 栋，276 对双喷淋有 26 对达标。说明等待修复没有改变平衡基线。

### 浏览器 QA（AI 操作，不是真人试玩）

采用独立本地生产预览端口 4177，路径 `/mora-fireline/`，未使用已有游戏来源的存档。

| 检查 | 实际结果 |
| --- | --- |
| 默认布局 1× 自然播放 | 13.9 秒时仍在播放，11 栋燃烧、1 栋烧毁；随后自动结算 0 / 12，显示 19.6 秒全毁及完整 45 秒口径 |
| 自动结果与详细复盘 | 两处均显示自动收束说明；首起火 C3、3.7 秒，前四条事件仍可见 |
| 1280×720 结果画面 | 新说明、首起火提示与按钮均在弹层内，无裁切 |
| 原样重试与暂停 | 重置到预警；6.6 秒暂停后再次读取仍为 6.6 秒 |
| 暂停时手动查看结果 | 0 / 12，与上次差值 0；不残留自动收束提示 |
| 修改后完整 2× 播放 | 键盘网格铺 B2–B7，花费 6 点；未点查看结果，正常得到 12 / 12，比上次多 12，无全毁提示 |
| 浏览器日志 | 本轮读取 warning / error 为空 |

仍未进行真人首次体验、手机、低端性能或公网发布验收。没有精确记录本轮有效工时。


## 2026-09-16 · v0.2.0-dev.3 六边形山谷与镜头

### 实际改动

- 延续本地尚未提交的全毁收束改动，未覆盖前一轮工作。用户明确授权六边形、可调镜头、自然远景与去棋盘化优化。
- src/sim/hex.ts 统一奇数行偏移坐标、六邻接、六边形距离与世界坐标；火势传播、喷淋范围、3D、拾取和辅助网格同步更新。新增固定第 4 行岩带，两片各 6 栋住宅。
- 删除厚方形底座、默认格线及常驻坐标；加入连续草地、砂土带、低矮岩带、住宅外观差异、焦木烟火、实际工作喷流。src/landscape.ts 生成外部沙地、林缘和雪峰。
- 透视镜头支持滚轮远近、右键拖动环视/俯仰、按钮调节与复位；防火带可左键拖动逐格铺设。旧方格存档保留，新地图使用独立键。
- 版本及锁文件元数据更新为 dev.3，没有更换依赖。规则、排期、接续、美术来源与测量脚本同步更新，新增文档 20 与新测量 JSON。

### 自动检查与数据

- 26 / 26 检查通过，类型与生产构建通过。六边形专项检查包含邻接对称、等距、边界与全部 4096 对地块距离和 BFS 最短路径一致。
- 测量 18 个单喷淋位置、153 对双喷淋、32 种合法西侧防线子集；参考布局 0/6/12 栋。原始数据保存在 evidence/triage-hex-v2.json，旧方格证据未覆盖。
- 五格完整防线花 5 点满保；B2/B6 双喷淋花 8 点、占两格满保；成本优势仍明显，不能将有多解写成平衡完成。
- 默认六边形局最后住宅在 19.2 秒烧毁，自动收束检查按新地图更新；完整模拟仍为 450 步。

### 浏览器 QA（AI 操作）

独立预览来源端口 4178、生产子路径 /mora-fireline/；在 1280×720 横屏检查，另见窗口等比留边。旧预览来源 4177 未用于本轮布局验收。

| 检查 | 实际结果 |
| --- | --- |
| 镜头按钮与滚轮 | 拉近、俯视、低视角、复位和滚轮缩放均改变画面；规划格线开关正常 |
| 变换镜头后的鼠标拾取 | 放大并俯视后拖过 B2/B3，预算 12→10；悬停显示 B3 防火带 |
| 连续建造与撤销 | 撤销后一格预算回 11；点击原位置补回后为 10，住宅始终 12 |
| 六边形键盘地图 | 错行六边形显示；天然岩带不可编辑；B6 成功放置喷淋 |
| 局部保护完整播放 | B2/B3 两格防线在 1× 正常结束保住 6 栋，未误触发全毁收束 |
| 混合方案完整播放 | 保留防线加 B6 喷淋，总花费 6；正常结算保住 12，显示比上次多 6 |
| 画面与日志 | 格线关闭后显示连续草地与自然远景，覆盖与坐标能按需显示；读取 warning/error 为空 |

首轮发现初始工具未设置场景 data-active-tool，导致不切工具就拖动时没有铺设；启动时统一 selectTool 后复测通过。最后修正住宅变体在模拟开始时的高度保留，补类型/构建和定向浏览器回归。

截图：evidence/hex-valley-v2.png。右键环视已接入，但本轮自动浏览器工具没有执行右键拖动验收；真实触屏、低端性能和真人体验也仍待验证。无公网部署，无精确有效工时。工具核验时间点 23:45:16（Asia/Shanghai），不是本轮起止区间。

## 2026-09-17 · 性能与身份目标

已实现增量建造、远景合批、静态阴影缓存、按步更新住宅状态、按需合并传播路径和拾取简化；加入标准/流畅画质。规划绘制调用 810→345、CPU P95 8.8→5.5ms；演练 CPU P95 8.9→6.5ms。内置浏览器帧率仍约 32，未证明用户设备掉帧完全解决。

加入 npm run test:perf 与 npm run check：26 项规则和 4 项性能结构检查全部通过，CPU 完整模拟 60 样本 P95 5.54ms（门槛 100ms），类型检查和生产构建通过。Three.js 分包体积提示仍存在。结构/CPU 自动回归不代替真实浏览器 GPU 测试。

身份迭代为“松风镇防灾规划员”，标题、常驻委托、演练文案和结算回扣 8/12 户目标；规划界面使用深蓝与金色，提高与自然场景的区分。AI 浏览器检查标题和规划布局无溢出；五格建造预算 12→7，增量更新 0.7–1.6ms。详见 21-性能回归与玩家身份迭代.md 与 evidence/performance-*.json。无真人试玩、无提交推送或部署。

补充浏览器 QA：五格防线正常完整演练后保住 12/12，使用 5/12 预算，结算显示完整守护达成；保留布局返回建造成功（该次全场景恢复 31ms）；菜单切换显示“流畅（关闭阴影）”；warning/error 日志为空。截图 evidence/patrol-ui-v1.png。真实鼠标拾取与传播路径显示在本轮性能修改后仍需专项复测，未记为已验收。

## 2026-09-17 · 海岸、雪山融水河与公开发布

用户明确授权将当前改动提交 GitHub、仓库转公开并部署 GitHub Pages。新增前景海岸与白色拍岸浪花，小镇东侧河流从雪山方向流向海口；水域仅作场景表现，不改变六边形火灾规则。两张持久网格使用共享时间参数，减少动态对象分配；减少动态效果偏好下冻结水纹。

31 项规则/性能结构检查通过；CPU 基准 P95 3.93ms、最大 5.90ms，类型与生产构建通过。浏览器检查水景位置、着色器正常，warning/error 为空；水面实际 GPU 性能及真人感受仍待复测。新增 Actions 工作流，发布前自动运行 npm run check。

发布验收：功能提交 880c66e 已推送 main；GitHub API 确认 private=false。Actions 35121755559 的 build/deploy 均成功，Pages 返回 HTTP 200。AI 浏览器从公开 URL 进入，默认演练结算 0/12；返回建造在 B2 铺设防火带，刷新后预算仍为 11，存档恢复成功。控制台 warning/error 为空。线上截图 evidence/coast-pages.png。此次为桌面 AI 浏览器验收，不是真人试玩或低端设备验收。

## 2026-09-17 · 卡通水景融合修订

检索 GitHub 的 toon water shader，并阅读 Roystan 的 Toon Water 教程：https://roystan.net/articles/toon-water/；对应源码 https://github.com/IronWarrior/ToonWaterShader 。参考浅深水渐变、岸边泡沫和噪声打散方法，自行编写 GLSL，未导入示例源码、纹理或 Unity 资源。

旧实现的问题是河海两张色带叠加、河口接缝、规则斜线水纹和草地直接切到水面。现在用统一世界坐标水域合并河海，海口逐渐拓宽；新增干沙—湿沙—浅水过渡，白色泡沫按噪声打散并随波峰推进消退；河流改为低对比短水纹，统一偏灰绿的环境色。固定一张网格，共享时间参数，不逐帧创建几何；使用岸线距离近似深度，没有额外深度或反射渲染通道。相比参考的真实深度方案，无法自动为任意入水物体生成交界泡沫。

31 项自动检查、类型与构建通过；CPU 完整模拟 P95 3.14ms、最大 5.22ms，此数字不代表 GPU 水面成本。浏览器最终视觉与采样另见 evidence/water-blend-*。本轮不改规则，尚无真人视觉验收。

性能补充：首轮逐片元程序噪声采样在 1280×720、DPR 1、双喷淋布局中得到 CPU P95 13ms，FPS 32；因此改用初始化时生成的 64×64 可重复噪声纹理，避免每像素多次三角函数哈希。纹理约 16 KiB，无外部图片依赖。最终数据单独保留；与早期默认布局、不同会话的 CPU 数据不作严格同条件比较。

最终纹理噪声版：规划阶段 600 帧，FPS 31.9、帧间隔 P95 31.7ms、CPU P95 10.3ms，350 次绘制调用；控制台 warning/error 为空。采样包含已有双喷淋布局，不能据此宣称所有设备流畅。截图 water-blend-final.png。更新已有 Pages 分享版本，沿用此前用户的发布授权。
